export enum ThemeEnum {
    light = 'light',
    dark = 'dark',
}

export interface ITheme {
    colors: {
        white: string,
        accentColor: string,
        background: string,
        textSecondary: string,
        elementsGrey: string,
}}
